## 백준 11720 문제 

split()은 숫자들을 **공백 기준으로** 입력받을때.

map()은 원소를 변환해주는 것.

### 공백 없이 숫자를 입력받을 때?

```
num=input()

만 해주면 input이 문자열로 받기 때문에, 리스트처럼 인덱싱이 가능하게된다.

즉 num[i]로 반복문 돌려서 각각 원소들을 합하거나 할 수 있음.

```

```
정답코드
n=int(input())

num_list=input() #문자열로 입력받게됨

total = 0

for i in range(n):
    total +=int(num_list[i]) #문자열을 인덱싱

print(total)
```
